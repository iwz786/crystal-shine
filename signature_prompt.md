Read the index.html file in the current directory and analyze the branding, logo path, colors, fonts, company name, contact details, and website URL.

Create a professional HTML email signature with the following requirements:

Use a clean table-based layout (email-client friendly).

Use only inline CSS (no external styles or <style> blocks).

Use images (such as logo) with their relative HTTP URLs from my website (e.g., https://mywebsite.com/images/logo.png, not local file paths).

The structure should be:

First row: A full-width row where I can write my message (placeholder text like: "Your message goes here").
Second row and below: Signature details formatted beautifully.
The signature must include:

Logo (aligned neatly, professional spacing)
Name: Kaizar M.
Job title (if found in index.html, otherwise leave placeholder)
Company name
Phone number
Email address
Website URL
Physical address (if available)
Make it:

Modern and minimal
Professional
Properly spaced
Compatible with Gmail, Outlook, and other major email clients
Max width around 600px
Mobile-friendly (stack logo and text if needed using simple table structure)
Ensure:

Fonts are web-safe (Arial, Helvetica, sans-serif)
Links are clickable (tel:, mailto:, https://)
No JavaScript
No external CSS
No SVG (unless widely email-compatible)
Optimized image sizing
Return only the final HTML code for the email signature.